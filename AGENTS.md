# 沟通

- 使用 caveman 模式
- 始终使用中文回复，可穿插少量英文术语

# 规范

- 如果你在工作过程中产出了过程文件 (草稿，测试脚本，测试结果)，使用完成后删除

# 安全

- 不可使用 git 命令改变仓库的工作区状态
- git diff 的结果是不准确的，因为仓库在 windows 上

# 工程规范

- `github.io/` 是 release 仓库，保持改动生产安全并充分验证。

### 工程质量门禁

- CSS tokens：避免不必要的 `var(--x)` 转发链；在可行时直接消费源 token。
- 配置安全：避免可能隐藏无效配置的默认参数回退；优先快速失败行为。
- 运行时路径：简化关键渲染和加载路径；避免在页面级代码中做不必要的工作。

### 测试卫生门禁

- 保持 `tests/` 最小化和稳定。
- 仅保留仓库通用护栏测试；计划私有的或一次性的重构检查在计划完成后移除。
- 不要在仓库中保留临时诊断脚本。
- 相关验证完成后移除一次性测试辅助函数。

### 验证门禁

- 使用基于风险的验证；当改动不可能影响运行时行为时，不运行昂贵检查。
- 仅文档改动（如 README/纯文本文档）：跳过 `bun run check` 和 `bun run test:site`。
- 代码、配置、样式或路由改动：运行 `bun run check` 和 `bun run test:site`。
- 如果失败是预先存在的，在报告中明确区分于新回归。

### README 门禁

- 仅使用英文。
- 除非改动明确替换，否则不要移除现有强制约束。
- 每次项目变更后，当项目实际情况变化时更新 README 章节（如 `Project Layout`）。
